# Wails / frontend build artifacts
build/bin
node_modules
frontend/node_modules

# Vite output — always (re)generated by `wails build` / `wails dev` before the
# Go compile, so it is never committed. (A bare `go build` here needs one build
# first to satisfy //go:embed all:frontend/dist; see README.)
frontend/dist

# wails' npm-install cache marker
frontend/package.json.md5
